/**
 * Index file untuk Firestore repositories
 */
const playlistRepository = require('./playlistRepository');
const musicbotRepository = require('./musicbotRepository');

module.exports = {
  playlistRepository,
  musicbotRepository
}; 